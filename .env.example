DATABASE_URL="postgresql://user:password@host/database"
NEBIUS_API_KEY="replace-with-a-nebius-token-factory-key"

# Optional: raises the public GitHub API rate limit for project source context.
GITHUB_TOKEN=""
